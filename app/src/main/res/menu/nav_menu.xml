<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/nav_home"
        android:layout_marginTop="10dp"
        android:icon="@mipmap/ic_home_foreground"
        android:itemBackground="@drawable/nav_item_background"
        android:title="Inicio" />

    <item
        android:id="@+id/nav_shop"
        android:layout_marginTop="10dp"
        android:icon="@mipmap/ic_shop"
        android:itemBackground="@drawable/nav_item_background"
        android:title="Tienda" />

    <item
        android:id="@+id/nav_cart"
        android:layout_marginTop="10dp"
        android:icon="@mipmap/ic_cart_foreground"
        android:itemBackground="@drawable/nav_item_background"
        android:title="Carrito" />

    <item
        android:id="@+id/nav_juegoteca"
        android:layout_marginTop="10dp"
        android:icon="@mipmap/ic_library"
        android:itemBackground="@drawable/nav_item_background"
        android:title="Juegoteca" />
</menu>